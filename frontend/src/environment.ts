const dev = {
    context: 'http://localhost:8080/'
  };
  
  // const prod = {
  //   context: 'production location'
  // };
  
//   export const environment = process.env.NODE_ENV === "production" ? prod : dev;
export const environment = dev;